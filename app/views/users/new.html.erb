<%= form_with model: @user, url: users_path, method: :post, local: true do |f| %>
  <%= f.label :name, 'Name:' %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :email, 'Email:' %>
  <%= f.text_field :email %>
  <br>
  <%= f.submit 'Create New User' %>
<% end %>